import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Bookend app smoke test', (WidgetTester tester) async {
    // TODO: Add widget tests once the app stabilizes.
    expect(true, isTrue);
  });
}
